<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.licy.fontscale">

    <dist:module dist:instant="true" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".moudle.font.FontScaleActivity" />
        <activity android:name=".moudle.typeface.ModifyTypeFaceActivity" />
        <activity android:name=".moudle.font.ModifyScaleResultActivity" />
        <activity android:name=".moudle.typeface.TypeFaceActivity" />
        <activity android:name=".moudle.typeface.TypeFaceResultActivity" />
    </application>

</manifest>